To run the examples server, you need Ruby and the sinatra gem installed, and
then:

```bash
bundle
bundle exec puma
```

You can now browse the examples at [http://localhost:9292](http://localhost:9292)